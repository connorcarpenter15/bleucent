{
  "name": "leucent",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "compose:up": "docker compose -f infra/docker-compose.yml up -d",
    "compose:up:local-pg": "docker compose -f infra/docker-compose.yml --profile local-postgres up -d",
    "compose:down": "docker compose -f infra/docker-compose.yml down -v",
    "compose:logs": "docker compose -f infra/docker-compose.yml logs -f"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
